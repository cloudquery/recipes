kind: destination
spec:
  name: bigquery
  path: cloudquery/bigquery
  version: "v2.1.2"
  write_mode: "append"
  spec:
    project_id: ${HOMEBREW_BIGQUERY_PROJECT_ID}
    dataset_id: ${HOMEBREW_BIGQUERY_DATASET_ID}